[ "$CI" = "true" ] && exit 0
echo "▶ Running Playwright E2E tests..."
node node_modules/@playwright/test/cli.js test --reporter=dot
if [ $? -ne 0 ]; then
  echo "✗ E2E tests failed. Push aborted."
  exit 1
fi
