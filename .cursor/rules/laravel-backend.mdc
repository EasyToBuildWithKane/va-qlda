---
description: Laravel — Controller, Request, Policy, Enum, Service, routes VA-QLDA
globs: app/**/*.php,routes/**/*.php,database/**/*.php
alwaysApply: false
---

# Laravel Backend — VA-QLDA

## Controller (mỏng)

```php
public function store(StoreTaskRequest $request, Project $project): RedirectResponse
{
    $this->authorize('contribute', $project);
    // Project/Task mutations → Application Use Case
    return back()->with('success', 'Đã thêm công việc.');
}
```

- **FormRequest:** `authorize()` + rules; messages tiếng Việt trong `messages()`.
- **Resource:** format Inertia props; quyền `can` khi cần (`ProjectResource`).
- **Bulk import:** `DB::transaction`, max 200 rows, `Import*Request` mirror client.

## Application Layer

| Module | Pattern | Path |
|--------|---------|------|
| DailyReport | Use Case + Domain | `app/Application/DailyReport/`, `app/Domain/DailyReport/` |
| Project | Use Case | `app/Application/Project/` — Create, Update, Duplicate, Archive, LogWork |
| Task | Use Case | `app/Application/Task/` — Create, Patch, Update, BulkCreate, Import |
| Blocker, Bug, Feedback | MVC trực tiếp | Controller → Model |

## Options / shared data

- `app/Support/Options.php` — facade delegate.
- `app/Support/Options/EmployeeOptions.php`, `ProjectOptions.php`, `DepartmentOptions.php` — inject qua AppServiceProvider, có cache.
- Business constants: `config/business.php`.

## Enums

`app/Support/Enums/` — backed string enum + `values()`, `label()`, `options()` khi dùng ở UI.

## Activity & notifications

- Task/Blocker: `TaskActivityLogger`, `BlockerActivityLogger` sau mutation.
- Inbox: `NotificationService` / `NotificationDispatcher` — không duplicate logic activity.

## DailyReport (Clean Architecture)

- Controller → **Use Case** (`app/Application/DailyReport/`), không query phức tạp trong controller.
- Model domain: `App\Domain\DailyReport\Models\` (không nhầm `App\Models\Project`).

## Routes

- Nhóm prefix + dot name: `projects.tasks.store`.
- Static segments **trước** `/{id}`: `/daily-reports/review` trước `/{report}`.
- Route model binding tùy chỉnh trong `RouteServiceProvider` nếu tên param ≠ model (vd. `notification` → `AppNotification`).

## Policies

Map explicit trong `AuthServiceProvider` nếu model ngoài `App\Models`.

## Tham chiếu

`docs/API_STRUCTURE.md`, `docs/DATABASE_STRUCTURE.md`, `docs/FOLDER_STRUCTURE.md`, `.cursor/rules/docs-sync.mdc`
